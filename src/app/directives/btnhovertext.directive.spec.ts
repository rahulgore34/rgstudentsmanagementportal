import { BtnhovertextDirective } from './btnhovertext.directive';

describe('BtnhovertextDirective', () => {
  it('should create an instance', () => {
    const directive = new BtnhovertextDirective();
    expect(directive).toBeTruthy();
  });
});
